# Blugold Group Docs

Our documentation page is built using [Docusaurus](https://docusaurus.io/) for ease of use.

## Local Development

This site runs using [nodejs](https://nodejs.org/en/download)

Once you have nodejs and npm installed, navigate to the directory with this code and run 

```bash
npm install docusaurus
npm start
```

These commands install Docusaurus and then starts a local development server and opens up a browser window. Most changes are reflected live without having to restart the server.

## To-Do List:

- Add ALL our projects
- Fully flesh out out the details for each project wnsuring formatting consistancy
- Go more in depth with the lab configuration
- Add a assets/visuals page (Logo(s), color palette, fonts, etc. - Similar to [Example 1](https://styleguide.torproject.org/brand-assets/) and [Example 2](https://styleguide.torproject.org/visuals/))
